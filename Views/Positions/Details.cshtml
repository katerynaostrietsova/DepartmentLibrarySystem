@model LibraryWebMvc.Models.Position

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Position</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PositionName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PositionName)
        </dd>
    </dl>
</div>
<h4>Readers with this position</h4>
<hr />

@if (Model.Readers != null && Model.Readers.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Full Name</th>
                <th>Registration Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var reader in Model.Readers)
            {
                <tr>
                    <td>@reader.FullName</td>
                    <td>@reader.RegistrationDate</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No readers have this position.</p>
}
<div>
    <a asp-action="Edit" asp-route-id="@Model?.PositionId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
